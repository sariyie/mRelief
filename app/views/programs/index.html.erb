<%#= render 'layouts/back' %>
<div style="background:#f4f4f4">
<div class="container">
  <div class="col-md-8 col-md-offset-2" style="text-align:center; padding: 40px 0 40px 0">
    <h1>All Programs for Chicago & Illinois</h1>
    <p> Below are a list of the benefits that are currently offered for prescreening using  mRelief. Although these are not all the benefits the city of Chicago, and the state of   Illinois offers, we are working to provide you with as much as we can. Click on a benefit below to begin the pre-screening process.</p>
  </div>

<!-- City of Chicago Programs  -->
<div style="margin-bottom:60px">
  <% chicago_programs = Program.where(:location => "chicago") %>
  <% increment = 0 %>
  <div class="row">
    <div class="col-md-12" style="text-align:center; padding-bottom:20px">
      <h3>Programs Accessible in Chicago Community Service Centers</h3>
    </div>
  </div>

  <div class="row" style="padding: 0 0 40px 0">
  <% chicago_programs = Program.where(:location => "chicago") %>
  <% chicago_programs.each_with_index do |program, i| %>
    <% if i % 3 == 0 %>
      <div class="row">
    <% end %>
      <div class="col-md-4">
        <div class="thumbnail" style="min-height:230px" >
          <div class="caption" style="text-align:center; padding:0 0 20px 0;">
            <h3><%= program.name %></h3>
            <p style="padding:0 30px 0 30px"><%= program.description %></p>
            <p><a href="<%= program.url %>" class="btn btn-primary" role="button">Find Out If You Qualify</a></p>
          </div>

        </div>
      </div>
    <% if (i + 1) % 3 == 0 %>
      </div>
    <% end %>
  <% end %>
</div>


<!-- Illinois Programs  -->
 <div class="row">
    <div class="col-md-12" style="text-align:center; padding-bottom:20px">
      <h3>Programs Accessible in the State of Illinois</h3>
    </div>
  </div>
<div style="margin-bottom:60px">
  <% illinois_programs = Program.where(:location => "illinois") %>
  <% increment = 0 %>
  <% illinois_programs.each_with_index do |program, i| %>
    <% if i % 3 == 0 %>
      <div class="row">
    <% end %>
      <div class="col-md-4">
        <div class="thumbnail" style="min-height:230px">
          <div class="caption" style="text-align:center; padding:0 0 20px 0">
            <h3><%= program.name %></h3>
            <p style="padding:0 30px 0 30px"><%= program.description %></p>
            <p><a href="<%= program.url %>" class="btn btn-primary" role="button">Find Out If You Qualify</a></p>
          </div>
        </div>
      </div>
    <% if (i + 1) % 3 == 0 %>
      </div>
    <% end %>
  <% end %>
</div>

<br/>
   <div class="row">
    <div class="col-md-12" style="text-align:center; padding-bottom:20px">
      <h3>Programs Accessible in Suburban Cook County</h3>
    </div>
  </div>
  <% cook_county_programs = Program.where(:location => "cook county") %>
  <div class="row" style="padding: 0 0 40px 0">
  <% cook_county_programs.each_with_index do |program, i| %>
    <% if i % 3 == 0  %>
      <div class="row">
    <% end %>
      <div class="col-md-4">
        <div class="thumbnail" style="min-height:230px">
          <div class="caption" style="text-align:center; padding:0 0 20px 0">
            <h3><%= program.name %></h3>
            <p style="padding:0 30px 0 30px"><%= program.description %></p>
            <p><a href="<%= program.url %>" class="btn btn-primary" role="button">Find Out If You Qualify</a></p>
          </div>
        </div>
      </div>
    <% if (i + 1) % 3 == 0 %>
      </div>
    <% end %>
  <% end %>
</div>
</div>

